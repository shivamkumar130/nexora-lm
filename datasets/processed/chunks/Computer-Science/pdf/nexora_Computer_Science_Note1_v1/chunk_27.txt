Document: nexora_Computer_Science_Note1_v1
Chunk: 27
Start Word: 6656
End Word: 6976
Words: 320

= 0; dim3 numBIocks((N + 15)/16, (N + 15)/16);) if (row <N && coI<N){ for (int k = 0; k < N; k++) matrixMul<<<numBlocks, threadsPerBlock>>>(d_A, d_B,d_C,N); sum+=A[row * N +k] * B[k * N+ col]; C[row * N + col] = sum; Key Points -Each thread computes one cell of the result matrix C[row][col]. -2D grid and block dimensions (dim3) map naturally to matrix coordinates. -Demonstrates parallelism across rows and columns. -Ideal to visualize thread blocks as tiles of the matrix. 24 Example 1: Vector Addition in CUDA Perform element-wise addition of two vectors (A + B = C) in parallel using GPU threads. CUDA Kernel Host Code int N= 1024; int*d_A,*d_B,*d_C; int i = blockIdx.x * blockDim.x + threadldx.x; cudaMalloc(&d_A, N * sizeof(int)); if(i<N) cudaMalloc(&d_B, N * sizeof(int)); C[i]=A[i] + B[i]; cudaMalloc(&d_C, N * sizeof(int)); // Copy data Host -> Device cudaMemcpy(d_A, A, N * sizeof(int), cudaMemcpyHostToDevice); cudaMemcpy(d_B, B, N * sizcof(int), cudaMemcpyHostToDevice); // Launch kernel vectorAdd<<<(N+255)/256, 256>>>(d_A,d_B,d_C, N); // Copy result back Device -> Host cudaMemcpy(C, d_C, N * sizeof(int), cudaMemcpyDeviceToHost); 16 MPI vs Shared Memory (OpenMP) Feature MPI OpenMP Memory Model Distributed (each process has own) Shared (all threads access same) Communication Explicit message passing Implicit via shared variables. Scalability Excellent (thousands of nodes) Limited to single node Programming Effort Higher (explicit data movement) Lower (compiler directives) Fault Tolerancee Better (process isolation) Weaker (shared state issues) Basic MPI Program Structure. #include <mpi.h> #include <stdio.h> int main(int argc, char** argv){ int rank, size; "Every MPI program MPI_Init(&argc, &argv); follows a fixed sequence - initialize -> identify -> MPI_Comm_rank(MPI_COMM_wORLD, &rank); MPI_Comm_size(MPI_COMM_WORLD, &size); execute -> finalize." printf("Hello from rank %d of %d\n", rank, size); MPI_FinalizeO; return 0; Example: Send & Receive int rank, data = 0: MPI_Status status: MPI_Comm_rank(MPI_COMM_WORLD, &rank) if (rank = O) { data - 42. Prepare aata MPI_Send(&data, I, MPI_INT.) 1, 0, MPI_COMM_WORLD); printf("Rank O sent: %d\n", data);. else if(rank =-- D)) 0, 0, MPI_COMM_WORLD. &status); printf("Rank