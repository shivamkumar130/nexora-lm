Document: nexora_Computer_Science_Note2_v1
Chunk: 41
Start Word: 10240
End Word: 10560
Words: 320

SELECT STUDENT_NAME FROM STUDENTS WHERE STUDENT_NAME LIKE o%A OR A&'; 10 11 STUDENT_NAME Riya QUESTION 11: QUERY: SELEGT COUNT(DISTINGT GRADES.STUDENT_ID) AS TOTAL NUMBER OF_STUDENTS FROM GRADES JOIN STUDENTS ON STUDENTS.STUDENT_ID = GRADES. STUDENT_ID JOIN COURSES ON GRADES.COURSE_ID = COURSES.COURSE_ID WHERE MARKS > 70 OR COURSE_NAME = MathS'; OUTPUT: Result Gride FilterRows: QUESTION 12: QUERY: SELECT AvG(CPI) AS average_cpi FROM Students; OUTPUT: aver... 9.05... QUESTION 13: QUERY: 5 0 SELECT STUDENT_NAME FROM STUDENTS ORDER BY CPI DESC ILIMIT 5;I OUTPUT: STUDENT_NAME pratik Jamn shri KRish Riya QUESTION 14: QUERY: ALTER TABLE STUDENTS MODIFY MARKS FLOAT; QUERY: SELECT * FROM STUDENTS WHERE MIDDLE NAME LIKE 'AB%'; OUTPUT: NONE QUESTION 16: QUERY: ALTER TABLE Students ADD PRIMARY KEY (Student_id); ALTER TABLE Grades ADD FOREiGN KEY ((student_id) REFERENCES Students(student_id): GIVEN below are all the codes used: -- 1st question SELECT COURSE_NAME,MAX(MAX_ENROLL) FROM( SELECT COUNT(GRADES.COURSE_ID) AS MAX_ENROLL,COURSE_NAME FROM GRADES JOIN COURSES ON GRADES.COURSE ID = COURSES.COURSE ID GROUP BY COURSES.COURSE_ID HAVING COUNT(GRADES.COURSE ID) > 1) AS ENROLL TABLE GROUP BY COURSE_NAME; -- 2ND QUESTION SELECT COUNT(STUDENT_ID_MUL) FROM( SELECT COUNT(GRADES.STUDENT_ID) AS STUDENT_ID_MUL FROM GRADES GROUP BY GRADES.STUDENT ID ) AS STUDENT COUNT; - 3RD QUESTION SELECT STUDENT_NAME,MAX(MUL COURSES) AS MULTIPLE COURSES FROM( SELECT STUDENT_NAME, COUNT(DISTINCT GRADE) AS MUL_COURSES FROM GRADES JOIN STUDENTS ON GRADES.STUDENT ID = STUDENTS.STUDENT ID GROUP BY STUDENT_NAME HAVING COUNT(DISTINCT GRADE) > 1 )AS MUL GRADES GROUP BY STUDENT_NAME; SELECT INSTRUCTORS.INSTRUCTOR_NAME FROM INSTRUCTORS JOIN COURSES ON INSTRUCTORS.INSTRUCTOR_ID = COURSES. INSTRUCTOR_ID JOIN GRADES ON COURSES.COURSE_ID = GRADES.COURSE_ID GROUP BY INSTRUCTORS.INSTRUCTOR NAME HAVING COUNT(DISTINCT STUDENT_ID) > 1; -- 5TH QUESTION SELECT cpi, GROUP_CONCAT(student_name) AS students_with_same_cpi FROM Students GROUP BY cpi HAVING COUNT(student_id) > 1; SELECT INSTRUCTORS.INSTRUCTOR_NAME,COURSES.COURSE_NAME,AVG(GRADEPOINT.GRA DE POINT)*0.4 AS MEANGPA FROM INSTRUCTORS JOIN COURSES ON INSTRUCTORS.INSTRUCTOR ID = COURSES.INSTRUCTOR ID JOIN GRADES ON COURSES.COURSE_ID = GRADES.COURSE_ID GROUP BY INSTRUCTORS.INSTRUCTOR_NAME, COURSES.COURSE_NAME ORDER BY MeanGPA DESC LIMIT 1; -- 7TH QUESTION SELECT STUDENTS.STUDENT_NAME,AVG(GRADEPOINT.GRADE_POINT)*O.4 AS GPA FROM STUDENTS JOIN GRADES ON STUDENTS.STUDENT_ID = GRADES.STUDENT_ID JOIN Gradepoint ON GRADES.GRADE=