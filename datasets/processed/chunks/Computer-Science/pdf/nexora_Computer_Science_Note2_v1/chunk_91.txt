Document: nexora_Computer_Science_Note2_v1
Chunk: 91
Start Word: 23040
End Word: 23360
Words: 320

1 0 1 0 1 0 1 0 1 Plaintext bits: 10=1, 01=1, 10=1, 01=1, 10=1, 01=1, 00=0, 01=1, 10=1, 01=1 So plaintext (letters) = (\texttt{BBBBBBABBB}) - nine b's and one a (position 6 is a). This matches the high (P(b)=0.9).. Q3 - using DL oracle to factor (n=pq) when (\gcd(p-1,q-1)=2) (5) Setup (transcribed).. Let (n=pq) (distinct odd primes) and suppose (\gcd(p-1,q-1)=2). Let (\alpha\in\mathbb{Z}n^*) be an element whose order is (dfrac{\varphi(n)}{2}). (So the subgroup (\anglelalpha\rangle) has size (\varphi(n)/2).) We have an algorithm (A) that, given any (\beta\in\/angle\alpha\rangle), returns (a=V/og\alpha\beta) in the range (0\le a<\varphi(n)/2). use (A) to get (a=\log_lalpha\beta). Explain how to get (Avarphi(n)) (and factor (n)) from (a). Assume (p>3,q>3). Solution (standard argument). 1. Because (\beta=\alpha^e) and (a=\log_\alpha\beta) with (0\le a<\varphi(n)/2), we have \alpha^{e-a}\equiv 1\pmod n.. So the order (l\alphaI=\dfrac{\varphi(n)}{2}) divides (e-a). That is \frac{\varphi(n)}{2},l, (e-a). 2. Choose (e=n). Then (\dfrac{\varphi(n)}{2}\mid (n-a)). So (n-a) is a positive multiple of (\varphi(n)/2). 3. Show that under the hypotheses (particularly (p>3,q>3) and (\gcd(p-1,q-1)=2)) the only possibility is n-a = \varphi(n). (Outline of the small arithmetic argument: since (\varphi(n)=(p-1)(q-1)) is fairly close to (n=pq) for primes (>3), the only multiple of (\varphi(n)/2) Iying strictly between (0) and (n) that is compatible is (\varphi(n)). Hence (n-a=\varphi(n)).) Thus we recover (Avarphi(n)). 4. Having (\varphi(n)) we get the usual quadratic for (p+q): \varphi(n) = n - (p+q) + 1 \quad\Rightarrow\quad p+q = n-\varphi(n)+1. So the primes (p,q) are roots of X^2 -(p+q)X + n = 0, and we factor (n) by solving this quadratic:. p,q =\frac{(p+q)\pm\sqrt{(p+q)^2-4n}}{2} Thus factoring (n) is trivial once (Avarphi(n)) is known. (Alternatively: once you know (n-a=\varphi(n)) you compute (s=p+q) and then (\gcd(X-p,n)) etc This is standard.) 5. A direct factoring trick from the found value (d=n-a) is also to compute g = \gcd(\alpha^{d/2}-1,n). Because (\alpha^{d/2}\equiv\alpha^{\varphi(n)/2}\equiv -1\pmod p) but (+1\pmod g) (or vice versa), this gcd yields a nontrivial factor of (n). So factoring is immediate. Remark. The core idea: knowing discrete log