Document: nexora_Computer_Science_Note2_v1
Chunk: 75
Start Word: 18944
End Word: 19264
Words: 320

set as long as total CPU utilization 100%. If deadlines are not harmonically related, EDF is more effective at meeting tight. deadlines. 5. Detecting Starvation Can a system detect starvation?. Yes. How? By monitoring process waiting time--if a process waits excessively long compared to. others or exceeds a threshold, starvation is detected. How to handle? Increase its priority or ensure it is eventually scheduled (e.g., through aging) 6. Shared Variable B (P1 and P2). P1: text C = B - 1 B = 2 * C P2: text Each call doubles the process count. Number of unique processes: 4 Number of unique threads: Each process executes the same thread, so 4.. 8. Algorithms Favoring CPU-Bound Processes FCFS: Neutral to CPU/l/O SJF: Favors short CPU jobs (CPU-bound if they're short) SRTF: Favors shortest remaining CPU burst (favors CPU-bound if their burst is shortest). Round Robin: Favors I/O bound (frequent preemption helps short tasks) MLFQ: Can be tuned; generally favors I/O bound, but CPU-bound can be starved if not adjusted. 9. Preemptive Round Robin Scheduling Problem Given the table of processes (an actual Gantt chart, waiting/turnaround calculations necessary) but the query asks to construct the Gantt chart, compute turnaround time, and CPU utilization rate. Due to image layout, calculations omitted here, but the steps are: Sort by arrival, apply round robin with preemption Track completion times for each process; o Turnaround = Completion - Arrival o Waiting = Turnaround - Burst. CPU Utilization = [(Total burst time) / (makespan)] * 100% 10. Two-Process Critical Section Solution Prove/disprove if the algorithm (looks like Peterson's) satisfies: Mutual exclusion: Yes. Progress: Yes.. Bounded waiting: Yes 11. Rate-Monotonic Scheduling for P1 and P2 Justify if both can be scheduled if deadlines are start of next period. Calculate utilization: U=(Ei/Ti)U = \sum (E_i / T_i)U=(Ei/Ti) If U<n(21/n-1)U \leq n(2^{1/n}-1)Un(21/n-1) for n tasks -> qualified for RMS 12. LLF Scheduling Laxity = Deadline - Current Time - Time