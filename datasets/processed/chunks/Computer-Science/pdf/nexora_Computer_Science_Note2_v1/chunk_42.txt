Document: nexora_Computer_Science_Note2_v1
Chunk: 42
Start Word: 10496
End Word: 10816
Words: 320

cpi, GROUP_CONCAT(student_name) AS students_with_same_cpi FROM Students GROUP BY cpi HAVING COUNT(student_id) > 1; SELECT INSTRUCTORS.INSTRUCTOR_NAME,COURSES.COURSE_NAME,AVG(GRADEPOINT.GRA DE POINT)*0.4 AS MEANGPA FROM INSTRUCTORS JOIN COURSES ON INSTRUCTORS.INSTRUCTOR ID = COURSES.INSTRUCTOR ID JOIN GRADES ON COURSES.COURSE_ID = GRADES.COURSE_ID GROUP BY INSTRUCTORS.INSTRUCTOR_NAME, COURSES.COURSE_NAME ORDER BY MeanGPA DESC LIMIT 1; -- 7TH QUESTION SELECT STUDENTS.STUDENT_NAME,AVG(GRADEPOINT.GRADE_POINT)*O.4 AS GPA FROM STUDENTS JOIN GRADES ON STUDENTS.STUDENT_ID = GRADES.STUDENT_ID JOIN Gradepoint ON GRADES.GRADE= GRADEPOINT.GRADE GROUP BY STUDENTS.STUDENT_NAME ORDER BY GPA DESC LIMIT 1; -- 8TH QUESTION SELECT house, MAX(GPA) AS GPA FROM ( SELECT s.house, s.student_name, AVG(gp.grade_point)*0.4 AS GPA FROM Students s JOIN Grades g ON s.student_id = g.student_id. JOIN Gradepoint gp ON g.grade = gp.grade GROUP BY s.house, s.student_name. ) AS house_gpas GROUP BY house; -- 9th Question SELECT STUDENT NAME FROM STUDENTS WHERE GRADE = 'FF'; -- 10TH QUESTION SELECT STUDENT_NAME FROM STUDENTS WHERE STUDENT_NAME LIKE '%A' OR 'A&'; -- 11TH QUESTION FROM GRADES JOIN STUDENTS ON STUDENTS.STUDENT_ID = GRADES. STUDENT_ID JOIN COURSES ON GRADES.COURSE_ID = COURSES.COURSE_ID WHERE MARKS > 70 OR COURSE_NAME = 'Maths'; SELECT AVG(CPI) AS average_cpi FROM Students; -- 13TH QUESTION SELECT STUDENT_NAME FROM STUDENTS ORDER BY CPI DESC LIMIT 5; -- 14TH QUESTION ALTER TABLE STUDENTS MODIFY MARKS FLOAT; -- 15TH QUESTION SELECT * FROM STUDENTS WHERE MIDDLE_NAME LIKE 'AB%'; 16TH QUESTION ALTER TABLE Students ADD PRIMARY KEY (student_id); ALTER TABLE Grades ADD FOREIGN KEY (student_id) REFERENCES Students(student_id); cS302(Distributed Parallel 2025-26 STUDENT ID:202351130 Computing) Lab 7 Aim of the Lab:- To implement a distributed program using MPI where two students intermediate values and performing heavy computations.. (Student 1-202351130) (Student 2-202351173)-my friend Code: Operation_Staff_WiFi.cpp C hello mpi.c Order_ Processing.cpp @ Operation_Staff-WiFi.cpp2 Lab7 > C Operation_Staff&WiFi.cpp> #include <mpi h> #include <iostream> using namespace stdj typede? long long 1l; Struct Node 11 mul, add; 10 Node combine(Node a, Node b, 11 mod) Node res? res.mul = (a.mul b.mul) % mod; ces.add = (a.mul + b.add + a.add) % mod; return res; Node power(Node base, 11 exp,