Document: nexora_Computer_Science_Note2_v1
Chunk: 87
Start Word: 22016
End Word: 22336
Words: 320

= \texttt{A7}_{16}): 1. Find multiplicative inverse in GF(2^8) (poly x^8+x^4+x^3+x+1, hex 0x11b): The inverse of 0xA7 is 0x48. (You can verify by GF multiplication 0xA7 * 0x48 = 0x01.) 2. Affine transform (apply bitwise linear map + constant 0x63): For the 8-bit value (b) (here 0x48), output byte (s) has bits. s_i = b_i\oplus b_{(i+4)\bmod 8} \oplus b_{(i+5)\bmod 8} \oplus b_{(i+6)\bmod 8} \oplus b_{(i+7)\bmod 8} \oplus c_i, where (c = 0x63) Applying this to 0x48 yields 0x5C.. Thus (\operatorname{Sbox}(0xA7)=0x5C). So SubBytes(A7) = 5C. (If you want, I can show the bitwise computation step-by-step - multiplicative inverse bit-string then all five XORs for each output bit.). Problem 6 - Distinguish which black box implements DES Interpretation (standard): you are given two black boxes implementing keyed functions (F_1(k,m)) and (F_2(k,m)) (both accept a 64-bit key and 64-bit message and return a 64-bit block). One of these black boxes implements DES(_k(m)) (the real DES encryption with key (k) and message (m)); the other implements an arbitrary random function (f(k,m)). You can make (chosen) queries ((k,m)\mapsto \text{output}) and want to decide which box is DES Simple distinguishing idea (permutation test / birthday collision): Property of DES: For any fixed key (k), the map (m\mapsto\text{DES}_k(m)) is a permutation on 64-bit blocks (i.e. bijection - distinct plaintexts map to distinct ciphertexts). Random keyed function: For a truly random function (f(k,\cdot)) (for fixed (k)), the mapping from plaintexts to outputs is a random function (not necessarily a permutation); if you query enough different plaintexts under the same key, you will likely see collisions among outputs. Attack: For each candidate black box B: 1. Choose a fixed key (k_0).. 2. Query B(k_0, m) for many distinct plaintexts (m) (say collect (g) outputs). 3. Check whether any two outputs collide. o If no collisions are observed (over a very large set of distinct plaintexts), that suggests B is DES (since DES for fixed key is collision-free). o If collisions