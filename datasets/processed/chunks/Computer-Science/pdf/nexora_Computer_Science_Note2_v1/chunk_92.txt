Document: nexora_Computer_Science_Note2_v1
Chunk: 92
Start Word: 23296
End Word: 23616
Words: 320

(Avarphi(n)) is known. (Alternatively: once you know (n-a=\varphi(n)) you compute (s=p+q) and then (\gcd(X-p,n)) etc This is standard.) 5. A direct factoring trick from the found value (d=n-a) is also to compute g = \gcd(\alpha^{d/2}-1,n). Because (\alpha^{d/2}\equiv\alpha^{\varphi(n)/2}\equiv -1\pmod p) but (+1\pmod g) (or vice versa), this gcd yields a nontrivial factor of (n). So factoring is immediate. Remark. The core idea: knowing discrete log in the large 2-power subgroup lets you recover a multiple of (\varphi(n)/2) (by setting exponent (e=n)), and parity/size constraints force that multiple to be (\varphi(n)); then factorization follows. Q4 - DLP description, faster algorithm, and ZK proof sketch (6) Discrete Log Problem (DLP). In a finite cyclic group (G=\langle g\rangle) of known order (m), given (g) and (h\in G), the DLP is: find (x\in{0,\dots,m-1}) such that (g^x=h).. Algorithm better than exhaustive search. (Two standard choices.) Baby-step / Giant-step (Shanks). Let (m) be group order, set (m_0=\lceillsqrt m\rceil) Precompute baby steps ({g^j:0\le j<m_0}) in a table. Then compute (g^{-m_0 i} h) for (i=0,1,\dots) and Iook up matches. This finds (x=i m_0 + j) in time and memory (O(\sqrt m)). Pollard's Rho for Iogarithms. A randomised algorithm with expected time (O(\sqrt m)) and negligible memory. For multiplicative groups (large prime modulus) index-calculus methods can be faster in practice and subexponential in the prime size.. Zero-Knowledge proof of knowledge (based on DLP hardness).. A standard ZK protocol to prove knowledge of discrete log (x) for (h=g^x) is Schnorr's. protocol (3-move, honest-verifier ZK): 1. Prover picks random (r), sends commitment (t=g^r) 2. Verifier sends random challenge (c) (small integer or from a large challenge space).. 3. Prover responds (s=r + c x \pmod m). 4. Verifier checks (g^s loverset{?}{=} t\cdot h^c) Completeness, soundness (extractor from two transcripts with different challenges gives (x)) and zero-knowledge (a simulator can produce indistinguishable transcripts) are standard proofs If you'd like, I can: expand any of the steps (e.g. the small arithmetic detail in Q3