Document: nexora_Computer_Science_Note2_v1
Chunk: 10
Start Word: 2304
End Word: 2624
Words: 320

your instructor will expect. 1) Two different viewpoints (both valid) A. The textbook formula view (theoretical limit). Many texts use the sliding-window rule W \ge 1 + 2a,\qquad a=\frac{T_{prop}{T_{trans}} If you literally set the transmission time (T_{trans}=0) (the problem says "ignore transmission... delays"), then (a = T_{prop}/0 \to \infty). That makes (W \to \infty). So theoretically the required window to keep the sender always busy is infinite - i.e. there is no finite minimum window imposed by propagation vs transmission when transmission time is zero. B. The practical / finite-data viewpoint. You only need to send a finite total number of frames: 2,000,000 bits - 1,000 bits/frame = 2000 frames. So even if an infinite window would remove any protocol-driven idle time, you only need a window large enough to send all 200o frames without waiting. Therefore in practice the minimum useful window = 2ooo (no point in having larger window than the total frames to send). Both statements are therefore compatible:. literal formula with (T_{trans}=0) -> infinite window (mathematical limit), and 2) Protocol-specific constraints (important for the exam) When you give final protocol answers you should respect the protocol window/sequence-number constraints:. Go-Back-N (GBN) Sender window must satisfy (W_{GBN} \le 2^k - 1) (k = # sequence bits) To be able to transmit all 2000 frames without waiting you need (W_{GBN} \ge 2000) oThe smallest k with (2^k - 1 \ge 2000) is (k=11) because (2^{11}-1=2047). So practical choice: Sender window (W_{GBN}=2000) (or 2047 if constrained to powers of two minus one) Receiver window = 1 (GBN receiver only accepts in-order frames) Sequence-number bits (k = 11) Selective-Repeat (SR) SR requires (W_{SR} \le 2^{k-1}). Sender and receiver windows are typically equal. Need (W_{SR} \ge 2000) > require (2^{k-1} \ge 2000) > smallest (k) is 12 (since (2^{11}=2048) So practical choice: o Sender window (W_{SR} = 2000) Receiver window = 2000 Sequence-number bits (k = 12). 3) Final clear answers (what