Document: nexora_Computer_Science_Note2_v1
Chunk: 88
Start Word: 22272
End Word: 22592
Words: 320

among outputs. Attack: For each candidate black box B: 1. Choose a fixed key (k_0).. 2. Query B(k_0, m) for many distinct plaintexts (m) (say collect (g) outputs). 3. Check whether any two outputs collide. o If no collisions are observed (over a very large set of distinct plaintexts), that suggests B is DES (since DES for fixed key is collision-free). o If collisions are observed, B cannot be DES (since DES_k is injective). Complexity & comments: The birthday bound says collisions among 64-bit outputs appear after about (2^{32}) collisions with good probability against a random function. Thus the approach is practical in principle but requires about (2^{32}) queries and If the course question asks for "polynomial many queries" in a security parameter, note (2^{32}) is exponential in the key-size parameter if key-size is treated as the security parameter; however in practice the birthday attack is the standard distinguishing method. Alternative notes (theoretical):. Any keyed block cipher family (like DES) yields permutations for fixed key; a truly random keyed function (mapping keyxmessage to output) behaves differently and can be distinguished using collision statistics as above. There also exist structural properties (e.g., DES complementation property or weak keys) that could also be used to help distinguish with fewer queries if exploited cleverly. but the generic and robust distinguishing test is the permutation/collision test described above. If you want: I can expand any calculation step-by-step (e.g., show exact GF(2^8) XOR additions for MixColumns, or show the exact bitwise table for the AES S-box affine step).. QThanks - I solved all four questions shown in the picture. I'll be concise and show the key steps / reasoning for each part. Q1 - equal-difference property I AES without SubBytes (5+5+5) Definition (equal-difference property). A function (f:{0,1}^N\to{0,1}^N) has the equal-difference property if x_1\oplus x_2 = x_3\oplus x_4 \quad\Longrightarrow\quad f(x_1)\oplus f(x_2) = f(x_3)\oplus f(x_4) for all (x_1,x_2,x_3,x_4). f(x_1)\oplus f(x_2)=(\alpha x_1+\beta)+(\alpha x_2+\beta)=\alpha(x_1+x_2) because (\beta+\beta=0) in characteristic 2.