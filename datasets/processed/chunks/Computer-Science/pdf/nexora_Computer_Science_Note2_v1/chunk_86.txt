Document: nexora_Computer_Science_Note2_v1
Chunk: 86
Start Word: 21760
End Word: 22080
Words: 320

hence observed (c) gives no advantage to an attacker - all plaintexts equally likely.) Problem 4 - Compression function (h(m 1Im 2)=\text{AES-128}(m 1,m 2)).Is (h) collision-resistant and preimage-resistant? Set-up: domain is ({0,1}^{256}) (because (m 1) and (m 2) are each 128 bits) and range is ( {O,1}^{128}) (AES output). So (h:{0,1}^{256}\to{0,1}^{128}) Collision resistance:. are hard to find matters for collision resistance.. A generic birthday attack needs about (2^{128/2}=2^{64}) evaluations of the compression function to find a collision between two domain inputs. So the generic large. Thus:. Information-theoretically collisions exist.. o Computationally, collisions can be found with about (2^{64}) work using birthday method - so (h) is not strongly collision-resistant to the same level of security as a 128-bit function (the generic security is only 64-bit). If your security requirement is 128-bit collision resistance, (h) does not meet it.. work). Preimage resistance:. Pick an arbitrary (m_1). AES({m_1}) is a permutation on 128-bit blocks, so compute (m_2 = \text{AES}^{-1}{m_1}(y)). That yields a valid preimage in one AES-decryption operation. Hence preimage attack is trivial - you can always choose any key candidate (m_1) and decrypt (y) to get (m_2). So (h) is not preimage-resistant at all. Summary: not preimage-resistant (trivial inversion), and collision resistance is only as strong as a 64-bit security level (birthday attack), so it fails to provide ideal collision resistance. Problem 5 - SubBytes(A7) = 5C (hex). Prove it.. AES S-box is: take byte (x), compute multiplicative inverse (x^{-1}) in GF(2^8) (with 0 mapped to O), then apply a fixed affine transformation.. Compute for input (x = \texttt{A7}_{16}): 1. Find multiplicative inverse in GF(2^8) (poly x^8+x^4+x^3+x+1, hex 0x11b): The inverse of 0xA7 is 0x48. (You can verify by GF multiplication 0xA7 * 0x48 = 0x01.) 2. Affine transform (apply bitwise linear map + constant 0x63): For the 8-bit value (b) (here 0x48), output byte (s) has bits. s_i = b_i\oplus b_{(i+4)\bmod 8} \oplus b_{(i+5)\bmod 8} \oplus b_{(i+6)\bmod 8} \oplus b_{(i+7)\bmod