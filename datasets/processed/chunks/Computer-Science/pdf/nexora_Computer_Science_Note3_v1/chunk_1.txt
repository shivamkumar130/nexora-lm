Document: nexora_Computer_Science_Note3_v1
Chunk: 1
Start Word: 0
End Word: 320
Words: 320

State Space Search for the Rabbit Leap Problem: BFS vs DFS Comparison Your Name,Student Member, IEEE Abstract—This report models the Rabbit Leap problem as a state space search problem and investigates solutions using Breadth First Search (BFS) and Depth First Search (DFS). BFS guarantees optimality in terms of the fewest moves, while DFS provides a feasible solution but may not always be optimal. A comparison of the two algorithms in terms of solution quality, time, and space complexity is presented. I. PROBLEMSTATEMENT In the Rabbit Leap problem, three east-bound rabbits stand in a line blocked by three west-bound rabbits. They are crossing a stream with stones placed in a line. There is one empty stone between them. The rabbits can move forward one or two steps. They can jump over one rabbit if needed, but not more than one at a time. The objective is to determine whether the rabbits can successfully cross each other without stepping into the water. II. STATESPACEMODELING Each state is represented as a string of length 7, e.g., EEE_WWW, where ‘E‘ denotes an east-moving rabbit, ‘W‘ denotes a west-moving rabbit, and ‘ ‘denotestheemptystone. Initial state:EEE_WWW Goal state:WWW_EEE At each step, the empty position can be swapped with an adjacent rabbit (1 step move) or a rabbit two positions away (jump). The total search space is7!/(3!3!1!) = 140 distinct configurations. III. BFS SOLUTION BFS explores all states level by level until the goal state is found. Since BFS explores in increasing depth, the first time it reaches the goal guarantees the minimum number of moves. 1from collections import deque 3def bfs(start, goal): 4visited = set([start]) 5queue = deque([[start]]) 6while queue: 7path = queue.popleft() 8state = path[-1] 9if state == goal: 10return path 11for nxt in generate_moves(state): 12if nxt not in visited: 13visited.add(nxt) 14queue.append(path + [nxt]) Listing 1. BFS Implementation in Python Result:BFS found a solution in 15 moves, which is the optimal solution length. IV. DFS