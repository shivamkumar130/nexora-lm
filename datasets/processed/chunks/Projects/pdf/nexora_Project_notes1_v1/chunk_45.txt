Document: nexora_Project_notes1_v1
Chunk: 45
Start Word: 11264
End Word: 11584
Words: 320

diagrams and comprehensive project documentation. PMU counters provide low-level, accurate hardware metrics that are not available through normal OS tools.. It helps in:. o Performance tuning. o Debugging hardware-related issues o Profiling applications. 4. Tools & Technologies Required. 1. Linux Os (PMUs are well supported on Linux via perf or 1ibpfm) 2. Programming Language -> C, C++, or Python 3. Pmu Access Library -> o perf_event_open syscall (Linux native) o 1ibpfm4 (Performance Monitoring Library) 4. Compiler -> gcc or g++ 5. Root or CAP Sys ADMIN permissions (for accessing PMU hardware events 5. Working Principle 1. Select PMU events to monitor (e.g., MEM_LOAD_RETIRED.LATENCY, CPU_CLK_UNHALTED, etc.) 2. Open PmU counters using perf_event_open() or 1ibpfm. 3. Start recording hardware event values. 4. Run target workload or system tasks.. 5. Stop and read counters to get raw event counts.. 6. Convert counts to latency metrics (cycles per load/store, average memory latency) 7. Display results in human-readable format or log file.. 6. Implementation Steps Step 1: Identify PMU Events Use perf 1ist command to see available PMU events: perf list Example latency-related events:. MEM_LOAD_RETIRED.L1_MISS MEM LOAD_RETIRED.L2 MISS MEM_LOAD_RETIRED.L3_MISS MEM_LOAD_RETIRED.LATENCY Step 2: Write PMU Access Code. C example using perf_event_open:. #include <linux/perf_event.h> #include <sys/syscall.h> #include <unistd.h> #include <stdio.h> 1. Run the tool while performing different workloads: oFile copy oCPU-intensive computation oMemory stress (stress-ng) 2. Compare the latency readings under different loads.. 9. Challenges Requires root permissions to access PMU counters.. PMU events vary between CPU architectures (Intel, AMD, ARM). Raw counts must be converted into meaningful latency metrics. 10. Future Improvementst. Build a Gul dashboard to visualize latency changes over time.. Support multiple PMU events simultaneously.. Add network latency monitoring.. Integrate alerts for high latency spikes.. If you want, I can now convert this into a professional PDF project report with diagrams and tables so it looks ready for submission.. Do you want me to make that PDF?. CS 266:SOFTWARE ENGINEERING PROJECT Project Introduction: Retail