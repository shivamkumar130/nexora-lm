Document: nexora_Project_notes1_v1
Chunk: 15
Start Word: 3584
End Word: 3904
Words: 320

# Install dependencies sudo apt update sudo apt install build-essential git libncurses5-dev libssl-dev flex bison qemu -y. # Get Buildroot git clone https://github.com/buildroot/buildroot.git. cd buildroot # Start config menu make menuconfig # (Choose target: x86_64, BusyBox, minimal rootfs, etc.) # Build make -j$(nproc) # After build, run with QEMU qemu-system-x86_64 -kernel output/images/bzlmage l -append "root=/dev/ram0 console=ttyS0"1 -nographic -initrd output/images/rootfs.cpio. End of Step: You'll boot into a tiny Linux OS inside QEMU.. 2. Minimalist Web Server in C (Hello World HTTP) # Make project dir. mkdir ~/webserver && cd ~/webserver # Create server.c nano server.c Paste this minimal web server: #include <stdio.h>. #include <string.h> #include <stdlib.h> #include <unistd.h> #include <arpa/inet.h> int main(){ int sockfd = socket(AF_INET, SOCK_STREAM, 0); struct sockaddr_in addr = {0}; addr.sin family = AF_INET; addr.sin_port = htons(8080); addr.sin_addr.s_addr = INADDR_ANY; bind(sockfd, (struct sockaddr*)&addr, sizeof(addr)). listen(sockfd, 10); printf("Server running on http://localhost:8080\n"); while(1){ int client = accept(sockfd, NULL, NULL); char response[] = "HTTP/1.1 200 OK\r\n" "Content-Type: text/plain\r\n\r\n" "Hello, World!n"; write(client, response, strlen(response)); close(client); return 0; # Compile gcc server.c -o server # Run ./server Open browser -> http://localhost:8080 -> you should see Hello, World! 3. Simple Linux Kernel Module (Hello Kernel) # Make project dir # Create hello.c nano hello.c Paste this: #include <linux/module.h> #include <linux/kernel.h> #include <linux/init.h> static int _init hello_init(void){ printk(KERN_INFO "Hello, Kernel\n"); return 0; static void __exit hello_exit(void) { printk(KERN_INFO "Goodbye, Kernel!\n"); module_init(hello_init); module_exit(hello_exit); MODULE_LICENSE("GPL"); MODULE_AUTHOR("You"); MODULE_DESCRIPTION("Simple Hello World Module"); Create Makefile: obj-m += hello.o all: make -C /lib/modules/$(shell uname -r)/build M=$(PWD) modules clean: make -C /lib/modules/$(shell uname -r)/build M=$(PWD) clean Build + load:. make sudo insmod hello.ko. sudo rmmod hello. dmesg I tail -n 5 # Should show "Goodbye, Kernel!' You now have a working kernel module. Wrap-up Plan Day 1 AM: Buildroot -> boot tiny Linux in QEMU Day 1 PM: Minimal web server (Hello World HTTP) Day 2 AM: Kernel module (Hello World) Day 2 PM: Clean up, screenshot everything, document steps C? This