Document: nexora_Project_notes1_v1
Chunk: 73
Start Word: 18432
End Word: 18752
Words: 320

6. Write test script to fetch and validate 100 sample TLE entries. 7. Benchmark: measure time to ingest 10,000 TLE entries Deliverable: data_ingestion.py working, SQLite database populated with live TLE data, Redis caching active Day 3: SGP4 Orbit Propagation Tasks: 1. Create orbit_prop.py with SGP4 propagation function using sgp4 library 2. Implement parse_tle_lines helper to extract orbital parameters from TLE text. 3. Build propagate_tle function that returns ECl positions (x, y, z in km) and velocities (vx, vy, vz in km/s) for given timestamps 4. Create get position at time function that queries database for TLE and calls SGP4 5. Validate against NASA JPL Horizons data for ISS (norad_id 25544) at known. timestamps 7. Write unit tests comparing positions to known ephemerides Deliverable: orbit_prop.py validated against NASA data, propagation engine ready for 10,000+ satellites Day 4: Pair Filtering (O(n) Pre-Filter) Tasks: 1. Create conjunction_filter.py with distance-based pair filtering 2. Implement compute_distance function using NumPy (Euclidean distance between two 3D positions) 3. Build filter_pairs function with three-stage filtering. o Stage 1: Distance < 10 km (coarse filter) o Stage 2: Relative velocity > 5 km/s o Stage 3: Altitude difference < 50 km 4. Use Numba JIT compilation for speed optimization (@jit(nopython=True)) 5. Benchmark: measure filtering time for 10,000 vs 50,000 satellites 6. Optimize using KDTree spatial indexing for faster neighbor search 7. Validate: ensure filtering reduces pairs from 50M (n?) to ~1,000 (sparse) Deliverable: conjunction_filter.py optimized with O(n) complexity, pair filtering reduces. candidates by 99% Day 5: Conjunction Detection Engine Tasks: 1. Create conjunction_engine.py with min separation distance calculation 2. Implement compute_min_separation function using scipy.optimize.minimize with BFGS method 3. Build compute_collision_probability function using Monte Carlo with 1,000 samples per. pair 4. Sample TLE uncertainty (Bstar 5%, mean motion 0.0001, inclination 0.001) 5. Compute collision probability as count(dist < 1 km) / 1000 6. Test with historical conjunction (2009 Iridium-Cosmos collision) 7. Validate against NASA Conjunction Data Network (CDN) database 8.