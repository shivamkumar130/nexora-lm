Document: nexora_Algorithm_notes1_v1
Chunk: 10
Start Word: 2304
End Word: 2624
Words: 320

as the root. o Each vertex vvv is connected to its predecessor uuu, the vertex that first discovered vvv. BFS Algorithm The BFS algorithm, presented below, assumes that the input graph G=(V,E)G = (V, E)G=(V,E) is represented using adjacency lists. BFS uses additional attributes for each vertex: . u. color: Tracks the color of vertex uuu (white, gray, or black). u. d: Stores the distance from the source vertex sss to vertex uuu.. u.n: Represents the predecessor of uuu. If uuu has no predecessor (e.g., if uuu is the source or has not been discovered), u.t=NILu.tr = NILu.t=NIL. A queue QQQ is used to manage the set of gray vertices.. 1. for each vertex u in G.V - {s} 2. u.color = WHITE 3. u.d = 4. u.tt = NIL 5. s.color = GRAY 6.s.d = 0 7. s.t = NIL 8.Q = 0 9. ENQUEUE(Q, s) 10. while Q =: 11. u = DEQUEUE(Q) 12. for each v in G.Adj[u]: 13. if v.color == WHITE: 14. v.color = GRAY 15. v.d = u.d + 1 16. 17. ENQUEUE(Q,v) 18. u.color = BLACK Algorithm Explanation Initialization: o All vertices, except for the source sss, are set to white, their distance (u. d) is set to infinity, and their predecessors (u. t) are set to NIL. o The source vertex sss is set to gray, its distance is set to 0, and its predecessor is NIL. The queue QQQ is initialized with the source vertex sss. Main Loop: o The loop continues as long as there are gray vertices in the queue.. A vertex uuu is dequeued, and for each adjacent vertex vvv of uuu, if vvv is white. (not discovered yet), it is marked gray, its distance is updated, its predecessor is set to uuu, and it is enqueued. o After processing all adjacent vertices, uuu is marked black, indicating it is fully. explored. The algorithm continues until all reachable