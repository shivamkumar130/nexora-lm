Document: nexora_Algorithm_notes1_v1
Chunk: 2
Start Word: 256
End Word: 576
Words: 320

\in E II 0 & \text{otherwise} \end{cases} Great -- let's solve both parts.. (a) Collision-free planning algorithm (high level + practical steps) Idea. Plan in the configuration space ((theta_1,\theta_2)) rather than workspace ((x,y)- Obstacles in the workspace map to forbidden regions (sets of ((\theta_1,\theta_2)) where either link or the tool intersects an obstacle). Then find a path from the configuration(s) that place the tool at the start point (S) to a configuration(s) that place the tool at goal (D) using graph search (BFS/A* / Dijkstra / PRM / RRT, depending on requirements). Detailed algorithm steps 1. Robot model / forward kinematics. For link lengths (l 1,I_2) and joint angles (Atheta_1,\theta_2) (in radians):. x = I_1\cos\theta_1 + I_2\cos(\theta_1+\theta_2),\qquad y = I_1\sin\theta_1 + I_2\sin(\theta_1+\theta_2) 2. Discretize configuration space.. Choose a resolution for (\theta_1,\theta_2) (e.g. 1 for high precision, or 5%/10 for speed). Build a 2D grid of configurations ((\theta_1,\theta_2)) with (\theta_i\in[0,360^lcirc)). 3. Collision test for a configuration. For each ((\theta_1,ltheta_2)) grid cell:. Compute positions of the link segments: link1 from (0,0) to ((x 1,y 1)=(I 1\cos\theta 1, I 1\sin\theta 1)); link2 from ((x 1,y 1)) to end ((x,y)). Check whether the tool point or either link segment intersects any workspace tests). If intersects, mark that configuration as forbidden. 4. Construct adjacency graph.. o For each allowed configuration, connect to neighboring allowed configurations Euclidean in angle space) or uniform cost 1.. o This graph models feasible small motions of the motors.. 5. Find start/goal nodes. Compute inverse kinematics for start point (S) to get one or more ((Atheta_1,theta_2)) solutions that place the tool at (S). Map those to nearest grid nodes (only keep those not forbidden). Do the same for goal (D).. o If none of the IK solutions map to allowed nodes, expand search to nearby workspace points or refine discretization.. 6. Search for a path.. o Run BFS (if uniform cost), Dijkstra, or A* (recommended). Use a heuristic for A* such