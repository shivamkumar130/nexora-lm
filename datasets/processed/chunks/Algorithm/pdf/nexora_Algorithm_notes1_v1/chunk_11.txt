Document: nexora_Algorithm_notes1_v1
Chunk: 11
Start Word: 2560
End Word: 2880
Words: 320

gray vertices in the queue.. A vertex uuu is dequeued, and for each adjacent vertex vvv of uuu, if vvv is white. (not discovered yet), it is marked gray, its distance is updated, its predecessor is set to uuu, and it is enqueued. o After processing all adjacent vertices, uuu is marked black, indicating it is fully. explored. The algorithm continues until all reachable vertices from sss have been discovered and fully. explored. Time Complexity The time complexity of BFS is O(V+E)O(V + E)O(V+E), where VVV is the number of vertices and EEE is the number of edges. This is because each vertex is processed once and each edge is explored once. Space Complexity The space complexity is O(V)O(V)O(V) due to the storage of vertex attributes (color, d, and t) and the queue QQQ Shortest Paths in BFS At the start of the section, it was stated that BFS computes the distance to each reachable vertex in a graph G=(V,E)G = (V, E)G=(V,E) from a given source vertex seVs \in VseV. 1. Shortest-Path Distance: o Define the shortest-path distance O(s,v)\delta(s, v)(s,v) from the source vertex sss to any vertex vvv as the minimum number of edges in any path from sss to. o If there is no path from sss to vvv, then 6(s,v)=\delta(s, v) = \inftyo(s,v)=. oA path with length 8(s,v)\delta(s, v)8(s,v) is called a shortest path 2. Correctness of BFS for Shortest Paths: o To prove that BFS correctly computes the shortest-path distance for all reachable vertices, we must first establish an important property of shortest paths Here's a rewritten version for better clarity and understanding: ## **Depth-First Search (DFS)** (denoted as \(v \)) that still has unexplored edges. Once all edges of \(v \) are explored, DFS **backtracks** to the vertex from which \(v I) was discovered. This process continues until all vertices reachable from the initial source vertex are discovered. If there are any **undiscovered vertices**