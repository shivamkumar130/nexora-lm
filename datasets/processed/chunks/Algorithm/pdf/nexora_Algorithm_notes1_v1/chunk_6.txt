Document: nexora_Algorithm_notes1_v1
Chunk: 6
Start Word: 1280
End Word: 1600
Words: 320

the bit, there is a directed edge from the current state to the new. state (single-bit flip). Example important transitions (others similarly computed): From ((1,1,1)): (a 2=-2< -0.5) so unit2 flips -> ((1,-1,1)). Units 1 and 3 remain +1. Thus a single update can take ((1,1,1)\to(1,-1,1)). ((1,-1,1)): all excitations give (a_1=2,;a_2=-2,;a_3=2) which keep units as currently set > ((1,-1,1)) is stable (fixed point). So the state-transition diagram has every state with arrows (for each unit that would flip). pointing to states of lower energy; the network flows (under asynchronous updates) into one of. the local minima: ((1,-1,1)) or ((-1,1,-1)). ((1,-1,1)) is the global energy minimum.. (If you want, I can draw and return the explicit 8-node directed graph with arrows between the concrete states.) (b) Hebbian learning and showing (x^{(1)}) is an energy minimum for the single-pattern weight matrix Hebbian rule (for (m) stored patterns (x^{(1)},\dots,x^{(m)}), components (\pm1)) (off-diagonal weights; (w_{ii}=0)): w_{ij}=\sum_{k=1}^m x_i^{(k)} x_j^{(k)}\qquad(i\neq j) W=\sum_{k=1}^m x^{(k)}{x^{(k)}}^T - mI Show: for the special case (m=1) and (W = x^{(1)}{x^{(1)}}^T - I), the energy is minimized at (x=x^{(1)}) Take any binary state (x\in{\pm1}^n). Compute. x^T W x;=;x^T\big(x^{(1)}{x^{(1)}}^T - I\big)x ;=; (x^T x^{(1)})^2 - x^T x. Because (x^T x = n) (each component squared is 1) we have x^T W x = (x^T x^{(1)})^2 - n. Now ((x^T x^{(1)})^2 \Ie (x^{(1)T} x^{(1)})^2 = n^2) (Cauchy), with equality only when (x=\pm x^T W x +) (threshold term), maximizing (x^T W x) minimizes the (-\tfrac12) term. So (x^{(1)}) (or its negative) is a minimizer of (E(x)). If thresholds are zero (or chosen consistently), (x^{(1)}) is a stable energy minimum. Condition to store all (m) patterns using Hebb rule (no interference): For the multi-pattern case (W=\sum_{k=1}^m x^{(k)}{x^{(k)}}^T - ml) we can compute x^{(p)T}W x^{(p)} = n^2 - mn +\sum_{k\ne p} (x^{(p)T}x^{(k)})^2. If the stored patterns are mutually orthogonal (i.e. (x^{(p)T}x^{(k)}=0) for (k\ne p)), then the. cross terms vanish and each stored pattern yields the same