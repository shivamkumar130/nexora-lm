Document: nexora_Ai_notes1_v1
Chunk: 31
Start Word: 7680
End Word: 8000
Words: 320

as fractions instead of decimals, or. re-run with different interpretations of the "change in hand" probability (if you intended. that row to mean something else). Q: (a) Heuristic - proposal and properties Proposed heuristic. Let each node (n) have planar grid coordinates ((x_n,y_n)) measured in grid units (the dashed grid in the figure). Since each grid unit = 10 km, define. h(n);=; 10\cdot\sqrt{(x_n-x_G)^2+(y_n-y_G)^2}\quad\text{(km)} where ((x_G,y_G)) are the coordinates of the goal node (G). Why this is admissible. Any real path from (n) to (G) must physically cover at least the straight-line distance between (n) and (G). Therefore the true shortest-path cost (h^(n)) satisfies (h^(n)\ge) straight-line distance. So (h(n)\le h^*(n)) for every (n) i.e. (h) never overestimates and is admissible. Why this is consistent (monotone). For any edge ((u,v)) with actual edge cost (c(u,v) (the distance to travel from (u) to ()), the Euclidean distances obey the triangle inequality: \text{dist}(u,G) \le \text{dist}(u,v) + \text{dist}(v,G) Multiplying by 10 (km per unit) gives h(u) \Ie c(u,v) + h(v) provided (c(u,v)) is at least the straight-line distance between (u) and (v) (which holds if edge costs represent geometric path lengths). Thus (h) is consistent. (If an edge cost were defined in some non-geometric way and could be smaller than the. straight-line distance, the admissibility/consistency argument would need the corresponding check; for the usual planar-graph edge-length interpretation the Euclidean heuristic is both. admissible and consistent.) (b) Will A* with this heuristic find an optimal path from S to G? Yes. Because the heuristic is admissible and consistent, A* is guaranteed to return an optimal are nondecreasing so nodes are never needlessly reopened, and the first time A* pops the goal from the open set it has found an optimal path. (If you want, I can also compute the numeric Euclidean heuristic values for each node from their. grid coordinates and run a shortest-path/A* trace on the specific diagram to show the exact. sequence of node